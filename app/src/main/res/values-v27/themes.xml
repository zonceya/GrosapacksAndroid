<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Remove the conflicting Theme.GrosaPacks definition from here -->
    <!-- Keep only the SplashScreen theme -->
    <style name="Theme.App.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_splash_foreground</item>
        <item name="windowSplashScreenAnimationDuration">200</item>
        <item name="postSplashScreenTheme">@style/Theme.GrosaPacks.NoActionBar</item>
    </style>
</resources>